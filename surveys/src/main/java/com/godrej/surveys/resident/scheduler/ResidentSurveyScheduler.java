package com.godrej.surveys.resident.scheduler;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.annotation.Configuration;

//@Configuration
public class ResidentSurveyScheduler {

	private Logger log = LoggerFactory.getLogger(getClass());

	/*
	 * @Autowired private BaselineSurveyService baselineSurveyService;
	 * 
	 * @Scheduled(cron = "* 10 10 * * *") public void firstReminder() {
	 * if(log.isInfoEnabled()) { log.info("%s %s","Reminders - ",
	 * Calendar.getInstance().getTime()); }
	 * baselineSurveyService.sendReminderToAllRegions(); }
	 */

}
